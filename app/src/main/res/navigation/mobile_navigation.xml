<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_corsi">

    <fragment
        android:id="@+id/nav_corsi"
        android:name="com.example.ripetizioni.ui.corsil.CorsiFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_corsi" >
        <argument
            android:name="idSession"
            app:argType="integer" />
    </fragment>

    <fragment
        android:id="@+id/nav_prenotazioni"
        android:name="com.example.ripetizioni.ui.prenotazioni.PrenotazioniFragment"
        android:label="@string/menu_gallery"
        tools:layout="@layout/fragment_prenotazioni" />

</navigation>